NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Production site URL, used for SEO metadata (Open Graph/Twitter image
# resolution, metadataBase). Falls back to http://localhost:3000 in local
# development if unset.
NEXT_PUBLIC_APP_URL=
