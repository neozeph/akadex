export default function PomodoroPage() {
  return (
    <main className="space-y-6">
      <section className="rounded-[2rem] border border-border bg-card p-8 shadow-sm">
        <p className="text-sm font-semibold tracking-[0.25em] text-muted-foreground uppercase">
          Pomodoro
        </p>
        <h1 className="mt-4 text-3xl font-semibold">Focus sessions</h1>
        <p className="mt-3 max-w-2xl text-muted-foreground">
          We will build the timer, pause/reset controls, and session logging after
          the semester and task flows are connected.
        </p>
      </section>
    </main>
  )
}
